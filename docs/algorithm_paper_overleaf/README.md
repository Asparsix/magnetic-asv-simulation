# Algorithm study paper → PDF

## What this is
Research / study paper on:
- Bayesian grid search (\(1/r^3\) likelihood)
- Mutual-information hunt
- Inverse-cube dipole LM fit
- How the peer ASV uses the same models
- Parameter catalog + study questions

Matches `version_3` code exactly.

## Get the PDF
1. Overleaf → New Project → Upload Project
2. Upload **`docs/algorithm_paper_overleaf.zip`**
3. Main file: `main.tex` · pdfLaTeX · Recompile · Download PDF

Markdown: `ALGORITHM_STUDY_PAPER.md` (parent folder).
